[core]
	autocrlf = input
	excludesfile = ~/.gitignore

[credential]
	helper = cache --timeout=14400

[diff]
	algorithm = histogram

[fetch]
	prune = true

[merge]
	ff = only

[push]
	default = current

[include]
	path = ~/.gitconfig.local
